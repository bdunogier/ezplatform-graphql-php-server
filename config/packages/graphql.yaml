overblog_graphql:
    definitions:
        schema:
            query: Domain
        mappings:
            types:
                -
                    type: yaml
                    dir: "%kernel.project_dir%/config/graphql/types"
                    suffix: ~
